# Model artifacts

Reserved for runtime model weights or configs (`*.pth`, `*.json`). Those files are gitignored except this note. FESEM analysis is paired from `data/fesem_supervised/` (micrographs + text) rather than shipped models — see `data/fesem_supervised/README.md`.
