# Model artifacts

Trained weights and preprocess configs are written here at runtime (e.g. `fesem_supervised/`). This directory is gitignored except this file. FESEM training requires `pip install soil-analytics[ml]`.
