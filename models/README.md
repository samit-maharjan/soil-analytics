# Model artifacts

Trained weights and preprocess configs are written here at runtime (e.g. `fesem_supervised/`). This directory is gitignored except this file. Train from labeled images in `data/fesem_supervised/` (see that README). FESEM requires `pip install soil-analytics[ml]`.
